# Multisample GraphPCA examples

The v2 API exposes:

```python
from GraphPCA import Run_Hierarchical_Multi_GPCA
```

Before publishing this example, copy the exact validated function signature and
a small synthetic example from the frozen analysis code. Do not invent new
defaults during packaging.
